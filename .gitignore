.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.mypy_cache/
.env
*.db
*.log
dist/
build/
# Per-machine audit logs (created at runtime per cfg.audit_db path)
clade-*-audit.db
# Production tokens.json — NIKAD ne commitovati (secret)
deploy/tokens.json
# NAPOMENA: relay/tokens.json je DEV tokens committed za POC convenience —
# ovo su localhost-only test keys. Production deploy generise sveze ./scripts/gen-keys.sh
# i smesta u deploy/tokens.json (van git-a).
